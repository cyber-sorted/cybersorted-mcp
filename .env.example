# CyberSorted MCP Server — Environment Variables
# Copy to .env and fill in values

# GCP (Firestore + GCS access)
GCP_PROJECT=cybersorted-prod
FIRESTORE_DATABASE=database-uk-prod
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/gcp-sa-key.json

# Stripe (subscription validation)
STRIPE_SECRET_KEY=sk_live_xxx

# Server
ENVIRONMENT=prod
PORT=8080
