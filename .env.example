# CyberSorted MCP Server — Environment Variables
# Copy to .env and fill in values

# GCP (Firestore + GCS access via Workload Identity Federation)
GCP_PROJECT=cybersorted-prod
FIRESTORE_DATABASE=database-uk-prod
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/gcp-wif-config.json

# Server
ENVIRONMENT=prod
PORT=8080
